%h2 Bookmarks
- if @current_bookmarks.nil? || @current_bookmarks.empty?
  %h4 You have no items in your bookmarks
- else
  = button_to "Delete All Bookmarks", delete_all_bookmarks_path, method: :post

%table
  %tbody
  .product_listing_display
  - @current_bookmarks.each do |item|
    %tr
      %td
        .listing_img
          = link_to image_tag(item.product.image.url, :height => "400", :class => "card-img"), product_path(item.product.id)
        .product_alignment_fields
          %p.card-text= item.product.name
          %p.card-text= item.product.category
          %p.card-text= '$' + item.product.price.to_s
          %p= link_to "More about #{item.product.name}", product_path(item.product.id), class: "search_button"
          %p= button_to "Delete Bookmark?", delete_one_bookmark_path(item.id), method: :post, class: "search_button"

  %table#products.table-style
    %thead
      %tr
        %th Product Name
        %th Category
        %th Price
        %th More Info
        %th Delete Bookmark
    %tbody
      - @current_bookmarks.each do |item|
        %tr
          %td= item.product.name
          %td= item.product.category
          %td= '$' + item.product.price.to_s
          %td= link_to "More about #{item.product.name}", product_path(item.product.id)
          %td= button_to "Delete Bookmark?", delete_one_bookmark_path(item.id), method: :post

%h4= link_to "Return to products page", products_path